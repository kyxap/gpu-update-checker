package github.kyxap.com.gui.tray;

import github.kyxap.com.cmd.CmdWorker;
import github.kyxap.com.http.HttpWorker;
import github.kyxap.com.utils.StringParser;

import java.awt.event.ActionEvent;

public class ActionWorker {
//    public void actionPerformed(ActionEvent e) {
//        // Handle the "Check Driver Updates" action
//        Logger.log("Check Driver Updates clicked");
//        String latestVer = StringParser.extractVersionFromHtlmString(HttpWorker.get(uri));
//        String installedVer = CmdWorker.runCmd("nvidia-smi");
//        Logger.log("New version is available: " + !installedVer.equals(latestVer));
//        if (!installedVer.equals(latestVer)) popUpInfo("New version is available: " + latestVer);
//    }
//
//    // Simulate triggering the ActionListener
//    public void simulateAboutAction() {
//        // You can pass any source object (can be null) and an action command if needed
//        ActionEvent actionEvent = new ActionEvent(this, ActionEvent.ACTION_PERFORMED, "AboutActionCommand");
//        aboutActionListener.actionPerformed(actionEvent);
//    }
}
